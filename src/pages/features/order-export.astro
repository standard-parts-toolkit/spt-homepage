---
import FeaturePageLayout from '../../layouts/FeaturePageLayout.astro';
---

<FeaturePageLayout
  title="Order Export — SPT"
  description="Push Shopify orders to Email, FTP, or HTTP API destinations in the format your warehouse, ERP, or 3PL expects."
  eyebrow="Order export"
  headline="Send orders to the warehouse, ERP, or 3PL the way they already expect them."
  subhead="Push every paid order to Email, FTP/SFTP, or an HTTP API endpoint, formatted to match the system on the other side. Retries, deduplication, and an audit trail included."
  heroCaption="Admin destinations list + a sample exported file/payload."
  howItWorks={[
    { step: 'Define destinations', body: 'One or more Email, FTP, or API endpoints.' },
    { step: 'Map the payload',     body: 'Shopify order fields to your destination\'s format.' },
    { step: 'Choose triggers',     body: 'On paid, on fulfilled, on tag, or scheduled.' },
  ]}
  configurable={[
    'Destination type: Email, FTP/SFTP, HTTP API',
    'Format: CSV, XML, JSON, or fixed-width',
    'Trigger condition: order paid, fulfilled, tagged, in a specific channel',
    'Per-line-item routing (split a single order to multiple destinations)',
    'Retry policy and backoff',
    'Manual replay from the run log',
  ]}
  dataSources="Independent of catalog and P&I. Use any combination of modules you need."
  faqs={[
    { q: 'Will it retry if my endpoint is down?', a: 'Yes, with exponential backoff and a manual replay option.' },
    { q: 'Can I split one Shopify order across two destinations?', a: 'Yes, by line item, vendor, location, or any tag rule you define.' },
    { q: 'Do you keep an audit trail?', a: 'Every export attempt is logged with the rendered payload, response, and timestamp.' },
    { q: 'Does this carry the in-cart fitment record?', a: 'Yes. The vehicle captured at search travels with the order through export, so your fulfillment system sees the exact vehicle the shopper bought for.' },
    { q: 'Which tier includes this?', a: 'Order Export is part of the Pro tier ($300/month). The Free tier does not include outbound order export.' },
  ]}
/>
